---
/**
 * Redirect from /books/ to /other-recommended-reading/
 * Maintains backward compatibility with old URLs
 */

import { createRedirect } from '../utils/redirect';

return createRedirect(Astro, '/other-recommended-reading/', '/books/');
---
