#!/bin/sh

# remark runs report-only (lint-md uses `remark .`, no `-o`), so it never
# rewrites files and never introduces the backslash escaping that `remark -o`
# does. script/fix-lint is therefore unnecessary here — it survives only as a
# manual rescue if someone runs `remark -o` by hand.
npm run lint-md
