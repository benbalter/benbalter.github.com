#!/bin/sh
# Creates a new post with today's date
# Usage: script/new [post-slug]

date=$(date +"%Y-%m-%d")
file="_posts/$date-$1.md"

git checkout -B $1
touch "$file"
echo "---\ntitle:\ndescription:\n---\n\n" > $file
atom pwd "$file"
